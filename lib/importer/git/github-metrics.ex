defmodule Importer.Git.GithubMetrics do
  require Logger

  def start() do
    importer_name = "github-metrics"
    Logger.info("Starting the importer - #{importer_name}")
  end

  def loop_importer() do
    loop_importer()
  end
end
